<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Rangeman.MainPage">
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Search" Clicked="searchDevice"/>
            <ListView x:Name="DevicesList" ItemsSource="{Binding DeviceList}"
                      CachingStrategy="RecycleElement"
                      ItemSelected="DevicesList_OnItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding Name}"></Label>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- 
        <StackLayout Orientation="Horizontal">
            <Button x:Name="btnStatus" Text="Status" Clicked="btnStatus_Clicked"/>
            <Button x:Name="btnScan" Text="Scan" Clicked="btnScan_Clicked"/>
            <Label x:Name="txtErrorBle" Text=""></Label>
        </StackLayout>
        -->
        </StackLayout>
    </ContentPage.Content>
</ContentPage>