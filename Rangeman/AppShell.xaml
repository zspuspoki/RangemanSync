<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:e="clr-namespace:Rangeman"
             xmlns:local="clr-namespace:Rangeman"
             x:Class="Rangeman.AppShell">
    <TabBar>
        <Tab Title="Download">
            <ShellContent ContentTemplate="{e:MyDataTemplate local:MainPage}" Route="Download" />
        </Tab>
        <Tab Title="Map">
            <ShellContent ContentTemplate="{e:MyDataTemplate local:MapPage}" Route="Map" />
        </Tab>
        <Tab Title="Config">
            <ShellContent ContentTemplate="{e:MyDataTemplate local:ConfigPage}" Route="Map" />
        </Tab>        
    </TabBar>
</Shell>