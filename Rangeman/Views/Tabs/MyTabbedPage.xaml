<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:local="clr-namespace:Rangeman;assembly=Rangeman"
             x:Class="Rangeman.MyTabbedPage">
    <NavigationPage Title="Download">
        <x:Arguments>
            <local:MainPage BindingContext="{Binding MainPageViewModel}" />
        </x:Arguments>
    </NavigationPage>
    <NavigationPage Title="Map">
        <x:Arguments>
            <local:MapPage BindingContext="{Binding MapPageViewModel}" />
        </x:Arguments>
    </NavigationPage>
    <NavigationPage Title="Config">
        <x:Arguments>
            <local:ConfigPage BindingContext="{Binding ConfigPageViewModel}" />
        </x:Arguments>
    </NavigationPage>    
</TabbedPage>